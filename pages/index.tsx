import type { NextPage } from "next";
// import styles from "../styles/Home.module.css";
import { theme } from "./../styles/theme";

const Home: NextPage = () => {
  return <div style={{ color: theme.palette.primary.main }}>teste</div>;
};

export default Home;
